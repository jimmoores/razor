/*
 * symbol-aliases.c -- ELF symbol aliases for the useful library
 *
 * On ELF platforms (Linux, BSD), no symbol aliases are needed since
 * the occam compiler does not add a leading underscore prefix.
 * This file exists as a placeholder required by the build system.
 */
